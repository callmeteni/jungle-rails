<h1>New Category</h1>

<%= form_for [:admin, @category] do |form| %>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit 'Create Category' %>
  </div>
<% end %>
